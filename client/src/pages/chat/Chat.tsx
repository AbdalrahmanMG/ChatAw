import ChatPlaceholder from "@/components/chat/ChatPlaceholder";

const Chat = () => {
  return (
    <div className="hidden lg:block h-svh">
      <ChatPlaceholder />
    </div>
  );
};

export default Chat;
